name: Blockchain Agent
description: Check ETH wallet balance
instructions: |
  You are a blockchain data agent. Use the tools to fetch data and respond concisely.
steps:
  - name: Check Balance
    content: |
      Get the ETH balance for the configured wallet address. Report the address and balance in ETH. Nothing else.
connectors:
  - connection_name: Ethereum
    connection_type: web3
