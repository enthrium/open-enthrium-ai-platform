# NOTE: ServiceNow dedicated adapter coming soon.
# Currently falls back to generic REST — use rest-api type with manual endpoints for now.

connectors:
  - name: my-servicenow
    type: servicenow
    instance: your-instance.service-now.com
    username: admin
    password: secret

memory:
  - key: context
    value: "ServiceNow ITSM. Key tables: incident, change_request, sc_request, cmdb_ci."
