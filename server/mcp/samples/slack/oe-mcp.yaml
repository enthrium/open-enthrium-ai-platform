connectors:
  - name: my-slack
    type: slack
    botToken: xoxb-your-bot-token-here

# Setup:
# 1. Go to https://api.slack.com/apps → Create New App
# 2. OAuth & Permissions → Add scopes: channels:read, chat:write, search:read
# 3. Install to workspace → copy Bot User OAuth Token

memory:
  - key: context
    value: "Slack workspace connected. Can post messages, list channels, search messages."
