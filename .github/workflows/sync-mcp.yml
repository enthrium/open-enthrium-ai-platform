name: Sync → oe-ai-mcp-server

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  sync:
    if: github.repository == 'enthrium/enthrium-commercial'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Clone oe-ai-mcp-server
        env:
          PAT: ${{ secrets.WEBSITE_PAT }}
        run: |
          git clone https://x-access-token:${PAT}@github.com/enthrium/open-enthrium-ai-mcp-server.git /tmp/mcp-repo

      - name: Sync server/ (excl. cli/) and npm wrapper and root files
        run: |
          rsync -avc --delete \
            --exclude='cli/' \
            --exclude='mcp/npm/' \
            --exclude='node_modules/' \
            --exclude='storage/' \
            --exclude='.env' \
            --exclude='.env.example' \
            server/ /tmp/mcp-repo/server/
          rsync -avc --delete server/mcp/npm/ /tmp/mcp-repo/npm/
          cp yarn.lock /tmp/mcp-repo/server/yarn.lock
          cp server/mcp/README.md /tmp/mcp-repo/README.md
          cp server/mcp/CONTRIBUTING.md /tmp/mcp-repo/CONTRIBUTING.md
          cp server/mcp/LICENSE /tmp/mcp-repo/LICENSE
          cp server/mcp/SECURITY.md /tmp/mcp-repo/SECURITY.md
          cp server/mcp/CHANGELOG.md /tmp/mcp-repo/CHANGELOG.md
          mkdir -p /tmp/mcp-repo/.github/workflows
          cp server/mcp/.github/workflows/release.yml /tmp/mcp-repo/.github/workflows/release.yml

      - name: Commit, push and tag
        env:
          PAT: ${{ secrets.WEBSITE_PAT }}
          TAG: ${{ github.ref_name }}
        run: |
          cd /tmp/mcp-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${PAT}@github.com/enthrium/open-enthrium-ai-mcp-server.git
          git add -A
          git diff --cached --quiet || git commit -m "sync: ${TAG}"
          git push origin main
          git tag -f ${TAG}
          git push origin ${TAG} --force
